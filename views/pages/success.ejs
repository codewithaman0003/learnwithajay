<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registration Successful</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .success-animation {
            animation: bounce 2s infinite;
        }
        @keyframes bounce {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.1); }
        }
    </style>
</head>
<body>
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card border-success">
                    <div class="card-header bg-success text-white text-center">
                        <h2>🎉 Registration Successful!</h2>
                    </div>
                    <div class="card-body">
                        <div class="text-center mb-4">
                            <div class="success-animation">
                                <svg width="100" height="100" viewBox="0 0 100 100" class="text-success">
                                    <circle cx="50" cy="50" r="45" fill="none" stroke="currentColor" stroke-width="8"/>
                                    <path d="M30,50 L45,65 L70,35" fill="none" stroke="currentColor" stroke-width="8" stroke-linecap="round"/>
                                </svg>
                            </div>
                        </div>
                        
                        <h4 class="text-center text-success">Welcome to the Webinar!</h4>
                        
                        <div class="alert alert-info mt-4">
                            <h5>📧 Check Your Email</h5>
                            <p>We've sent a confirmation email with all the webinar details and access link.</p>
                        </div>

                        <div class="webinar-details mt-4">
                            <h5>Webinar Information:</h5>
                            <ul class="list-group">
                                <li class="list-group-item">
                                    <strong>Date & Time:</strong> [Webinar Date] at [Webinar Time] IST
                                </li>
                                <li class="list-group-item">
                                    <strong>Duration:</strong> 60 minutes including Q&A
                                </li>
                                <li class="list-group-item">
                                    <strong>Meeting Link:</strong> Sent via email 1 hour before webinar
                                </li>
                                <li class="list-group-item">
                                    <strong>Preparation:</strong> Have your questions ready!
                                </li>
                            </ul>
                        </div>

                        <div class="mt-4 text-center">
                            <div class="btn-group">
                                <button class="btn btn-success" onclick="addToCalendar()">📅 Add to Calendar</button>
                                <button class="btn btn-info" onclick="shareWebinar()">📤 Share with Friends</button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script>
        function addToCalendar() {
            alert('Calendar feature would be implemented here!');
        }

        function shareWebinar() {
            if (navigator.share) {
                navigator.share({
                    title: 'Join this amazing webinar!',
                    text: 'I just registered for this webinar. Check it out!',
                    url: window.location.href
                });
            } else {
                alert('Share this URL: ' + window.location.href);
            }
        }
    </script>
</body>
</html>